本章目标字数（参考）: {target_chapter_words}

### 章节大纲（原文）
{outline}

### 输出格式（严格遵守）
只输出一个 JSON 对象，顶层键为 `atoms`。

```json
{{"atoms":[{{"id":"b1","intent":"整章执行任务：……","weight":1}}]}}
```

### 自检
- 是否只有 1 个 atom？
- intent 是否要求完成整章故事单元，而不是拆成多个节拍？
- intent 是否明确了目标、阻碍、行动、转折、兑现/代价、新期待？
- 是否避免了“按句子/段落切分章纲”？
